# Dependencies
node_modules/
.pnpm-store/
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
env/
target/
vendor/

# Build outputs
dist/
build/
out/
.next/
.turbo/
.vite/
*.tsbuildinfo

# Test + coverage
coverage/
.nyc_output/
.pytest_cache/
htmlcov/

# Environment + secrets
.env
.env.local
.env.*.local
*.pem
*.key
*.crt
*.p12

# IDE / editor
.vscode/
.idea/
*.swp
*.swo
*~

# Claude Code local state
.claude/

# OS
.DS_Store
Thumbs.db
desktop.ini

# Logs
*.log
logs/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Telephony runtime
freeswitch/log/
freeswitch/db/
freeswitch/recordings/
kamailio/dispatcher.list

# AI models + training artifacts
*.safetensors
*.gguf
*.bin
models/
checkpoints/
training-data/
rag-index/

# .dkb bundles (large + may contain sensitive metadata)
*.dkb
bundles/

# Recordings (never commit). Storage directories live at the
# repo root or under /var/lib in deploy targets; source code
# directories named "recordings" (e.g. app/api/cluster/recordings
# from iter 113) are fine. Use leading slash to anchor.
/recordings/
data/recordings/
var/lib/dialeros/recordings/
*.wav
*.mp3
*.opus
*.flac

# Local dev data + sqlite
data/
*.db
*.db-journal
*.db-shm
*.db-wal

# Database dumps
*.sql.gz
*.dump
backup/

# Ansible per-run inventory contents (keep dir via .gitkeep)
infra/ansible/inventory/*
!infra/ansible/inventory/.gitkeep

# Python
*.egg-info/
.tox/
.mypy_cache/
.ruff_cache/

# Local infra
.terraform/
*.tfstate
*.tfstate.backup
docker-compose.override.yml
